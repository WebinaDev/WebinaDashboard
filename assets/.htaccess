# Cache hashed dashboard build assets aggressively (filenames change on deploy).
<IfModule mod_headers.c>
	<FilesMatch "\.(?:js|css|woff2?|svg|map)$">
		Header set Cache-Control "public, max-age=31536000, immutable"
	</FilesMatch>
</IfModule>
