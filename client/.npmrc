; npm / npx downloads (registry + tarballs) — HTTP CONNECT on local proxy
; اگر فقط SOCKS روی این پورت دارید، کلاینت را طوری تنظیم کنید که HTTP هم روی همین پورت یا پورت دیگر باز کند.
proxy=http://127.0.0.1:10808
https-proxy=http://127.0.0.1:10808
