#!/usr/bin/env bash
# Deprecated in three-repo architecture.
set -euo pipefail

echo "Modules are no longer built in Dashboard repository."
echo "Build and package each module in its own repository via CRM release pipeline."
echo "See: webinocrm/docs/module-release-pipeline.md"
